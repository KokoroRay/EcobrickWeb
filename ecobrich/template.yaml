AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: Plastic Reward System - Serverless Backend

Globals:
  Function:
    Timeout: 10
    MemorySize: 128
    Runtime: provided.al2023
    Architectures:
      - arm64
    Environment:
      Variables:
        TABLE_NAME: !Ref PlasticDbTable
        USER_POOL_ID: !Ref PlasticUserPool

Resources:
  # ------------------------------------------------------------------
  # 1. DYNAMODB TABLE
  # ------------------------------------------------------------------
  PlasticDbTable:
    Type: AWS::DynamoDB::Table
    Properties:
      AttributeDefinitions:
        - AttributeName: PK
          AttributeType: S
        - AttributeName: SK
          AttributeType: S
      KeySchema:
        - AttributeName: PK
          KeyType: HASH
        - AttributeName: SK
          KeyType: RANGE
      BillingMode: PAY_PER_REQUEST

  # ------------------------------------------------------------------
  # 2. COGNITO USER POOL
  # ------------------------------------------------------------------
  PlasticUserPool:
    Type: AWS::Cognito::UserPool
    Properties:
      UserPoolName: PlasticUserPool
      AutoVerifiedAttributes:
        - email
      UsernameAttributes:
        - email
      Policies:
        PasswordPolicy:
          MinimumLength: 8
          RequireLowercase: true
          RequireNumbers: true
          RequireUppercase: true

  PlasticUserPoolClient:
    Type: AWS::Cognito::UserPoolClient
    Properties:
      ClientName: PlasticReactClient
      UserPoolId: !Ref PlasticUserPool
      GenerateSecret: false
      ExplicitAuthFlows:
        - ALLOW_USER_SRP_AUTH
        - ALLOW_REFRESH_TOKEN_AUTH

  # ------------------------------------------------------------------
  # 3. API GATEWAY
  # ------------------------------------------------------------------
  PlasticApi:
    Type: AWS::Serverless::Api
    Properties:
      StageName: Prod
      Cors:
        AllowMethods: "'GET,POST,OPTIONS'"
        AllowHeaders: "'Content-Type,X-Amz-Date,Authorization,X-Api-Key,X-Amz-Security-Token'"
        AllowOrigin: "'*'"
      Auth:
        # DefaultAuthorizer removed to allow OPTIONS to satisfy CORS without Auth
        Authorizers:
          CognitoAuthorizer:
            UserPoolArn: !GetAtt PlasticUserPool.Arn

  # ------------------------------------------------------------------
  # 4. LAMBDA FUNCTIONS
  # ------------------------------------------------------------------
  DonateFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: . 
      Handler: bootstrap
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref PlasticDbTable
      Events:
        DonateApi:
          Type: Api
          Properties:
            RestApiId: !Ref PlasticApi
            Path: /donate
            Method: POST
            Auth:
              Authorizer: CognitoAuthorizer
    Metadata:
      BuildMethod: makefile

  # ------------------------------------------------------------------
  # 5. ADMIN FUNCTIONS
  # ------------------------------------------------------------------
  AdminAwardFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: .
      Handler: bootstrap
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref PlasticDbTable
      Events:
        AdminAwardApi:
          Type: Api
          Properties:
            RestApiId: !Ref PlasticApi
            Path: /admin/award-points
            Method: POST
            Auth:
              Authorizer: CognitoAuthorizer
    Metadata:
      BuildMethod: makefile

  AdminUsersFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: .
      Handler: bootstrap
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref PlasticDbTable
        - Statement:
            - Effect: Allow
              Action:
                - cognito-idp:ListUsers
              Resource: !GetAtt PlasticUserPool.Arn
      Events:
        AdminUsersApi:
          Type: Api
          Properties:
            RestApiId: !Ref PlasticApi
            Path: /admin/users
            Method: GET
            Auth:
              Authorizer: CognitoAuthorizer
    Metadata:
      BuildMethod: makefile

  AdminDonationsFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: .
      Handler: bootstrap
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref PlasticDbTable
      Events:
        AdminDonationsListApi:
          Type: Api
          Properties:
            RestApiId: !Ref PlasticApi
            Path: /admin/donations
            Method: GET
            Auth:
              Authorizer: CognitoAuthorizer
        AdminDonationsReviewApi:
          Type: Api
          Properties:
            RestApiId: !Ref PlasticApi
            Path: /admin/donations/review
            Method: POST
            Auth:
              Authorizer: CognitoAuthorizer
    Metadata:
      BuildMethod: makefile

  VouchersFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: .
      Handler: bootstrap
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref PlasticDbTable
      Events:
        GetVouchersApi:
          Type: Api
          Properties:
            RestApiId: !Ref PlasticApi
            Path: /vouchers
            Method: GET
            # Public access to view available vouchers? Yes for now. Or Auth optional?
            # User wants to perform exchange, so listing needs to be available.
            # Let's make it authenticated to be safe, or open. 
            # Prompt implied integration with system. Let's start with Auth required for simplicity in consistent API.
            Auth:
              Authorizer: CognitoAuthorizer
        PostVouchersApi:
          Type: Api
          Properties:
            RestApiId: !Ref PlasticApi
            Path: /vouchers
            Method: POST
            Auth:
              Authorizer: CognitoAuthorizer
        OptionsVouchersApi:
          Type: Api
          Properties:
             RestApiId: !Ref PlasticApi
             Path: /vouchers
             Method: OPTIONS
    Metadata:
      BuildMethod: makefile

  RedeemFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: .
      Handler: bootstrap
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref PlasticDbTable
      Events:
        RedeemApi:
          Type: Api
          Properties:
            RestApiId: !Ref PlasticApi
            Path: /redeem
            Method: POST
            Auth:
              Authorizer: CognitoAuthorizer
        OptionsRedeemApi:
            Type: Api
            Properties:
               RestApiId: !Ref PlasticApi
               Path: /redeem
               Method: OPTIONS
    Metadata:
      BuildMethod: makefile

Outputs:
  ApiEndpoint:
    Description: "API Gateway endpoint URL"
    Value: !Sub "https://${PlasticApi}.execute-api.${AWS::Region}.amazonaws.com/Prod/"
  UserPoolId:
    Value: !Ref PlasticUserPool
  UserPoolClientId:
    Value: !Ref PlasticUserPoolClient